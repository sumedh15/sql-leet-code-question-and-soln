-- Create Scores Table

CREATE TABLE Scores (
    Id INT PRIMARY KEY,
    Score DECIMAL(3,2)
);

-- Insert Sample Data

INSERT INTO Scores (Id, Score)
VALUES
(1, 3.50),
(2, 3.65),
(3, 4.00),
(4, 3.85),
(5, 4.00),
(6, 3.65);

SOLUTION:
select
Score,
DENSE_RANK() over(order by score desc) as 'rank'
from scores
